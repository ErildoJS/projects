/**
 * 
 * tipos de dados no js se dividem em primitivos e objectos
 * 
 * primitivos - conhecidos por serem imutaveis o seu valor nao muda
 * existem 6 tipos primitovos: number string, bolean, symbol, null, undefined
 * 
 * typeof - serve pra vero tipo de dado envolvido
 * 
  esses tipos formam boxing - se forma em volta deles a ideia de um wrappper
  ex: (10).toFixed(2) == '10.00'

  -----------------------------------

  objectos - sao vlaores que representam uma referencia em memoria que pode ser alterada
   tipos: function, object [] regex new Date()
 */

typeof '23'